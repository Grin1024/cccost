{
	"name": "@mariozechner/cccost",
	"version": "1.0.2",
	"description": "Claude Code Cost Tracker - Track token usage and costs for Claude Code sessions",
	"bin": {
		"cccost": "dist/cli.js"
	},
	"scripts": {
		"clean": "rm -rf dist",
		"build": "tsc && cp src/interceptor-loader.js dist/ && chmod +x dist/cli.js",
		"check": "biome check --write . && tsc --noEmit",
		"prepublishOnly": "npm run clean && npm run build",
		"prepare": "husky"
	},
	"files": [
		"dist",
		"README.md"
	],
	"keywords": [
		"claude",
		"anthropic",
		"token",
		"usage",
		"cost",
		"tracker",
		"cli"
	],
	"author": "Mario Zechner",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/badlogic/cccost.git"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.0",
		"@types/node": "^22.10.5",
		"husky": "^9.1.7",
		"tsx": "^4.20.4",
		"typescript": "^5.9.2"
	}
}
